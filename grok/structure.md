# structure.py

## Purpose
Optional bounded rewiring from probe credit with confirmation streaks, reachability guard, and global-fitness gate (S17 lesson).

## Notes
Default off. Enable via `TrainConfig.structural_probe_gain > 0` or `--structural`.
